# Copyright 2026 上海如静知华信息科技有限公司
services:
  mysql:
    image: mysql:8.4
    environment:
      MY内容_DATABASE: ${MY内容_DATABASE:-zhuatech_bidagent}
      MY内容_USER: ${MY内容_USER:-zhuatech}
      MY内容_PASSWORD: ${MY内容_PASSWORD:-change-me-for-local-only}
      MY内容_ROOT_PASSWORD: ${MY内容_ROOT_PASSWORD:-change-root-password}
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    volumes:
      - bidagent_mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 5s
      timeout: 5s
      retries: 20

  backend:
    build: ./backend
    environment:
      DB_URL: jdbc:mysql://mysql:3306/${MY内容_DATABASE:-zhuatech_bidagent}?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
      DB_USERNAME: ${MY内容_USER:-zhuatech}
      DB_PASSWORD: ${MY内容_PASSWORD:-change-me-for-local-only}
      JWT_SECRET: ${JWT_SECRET:-replace-with-at-least-32-random-characters}
      CORS_ORIGINS: ${CORS_ORIGINS:-http://localhost:8090}
    depends_on:
      mysql:
        condition: service_healthy

  frontend:
    build: ./frontend
    ports:
      - "${WEB_PORT:-8090}:80"
    depends_on:
      - backend

volumes:
  bidagent_mysql_data:
